name: CI ASOLIMSEM

on: [push, pull_request]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Descargar codigo
        uses: actions/checkout@v4

      - name: Verificar integracion continua
        run: echo "Pipeline de ASOLIMSEM ejecutado correctamente"
